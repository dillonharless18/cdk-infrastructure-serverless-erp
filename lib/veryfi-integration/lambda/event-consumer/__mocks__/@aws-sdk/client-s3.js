// const mockSend = jest.fn(() => "https://bucket.s3.amazonaws.com/key");

// export const S3Client = jest.fn().mockImplementation(() => ({
//     send: mockSend,
// }));

// export const PutObjectCommand = jest.fn().mockImplementation((data) => data);
//
